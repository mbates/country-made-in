/**
 * Service worker. Deep-search orchestration and permission requests land here
 * in plan 05; for now it only proves the worker registers and starts.
 */
console.info('[country-made-in] service worker started')

export {}
